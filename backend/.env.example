AWS_BEARER_TOKEN_BEDROCK=
AWS_REGION=ap-south-1
BEDROCK_MODEL=google.gemma-3-27b-it
LLM_MAX_TOKENS=2048
RESUME_GENERATION_MAX_TOKENS=16384
RESUME_TARGET_PAGES=4.0
# When true (default), allow LLM polish for mashed text; length uses importance selection only.
RESUME_LLM_CONDENSE=true
# Optional slow multi-pass polish. Section quality gate already repairs critical issues.
RESUME_LLM_POLISH=false
# Multi-agent pipeline: extract -> plan -> write -> critique -> refine (best draft ships).
RESUME_AGENT_PIPELINE=false
AGENT_MAX_ROUNDS=2
AGENT_MAX_LLM_CALLS=8
AGENT_SCORE_THRESHOLD=85
AGENT_TIME_BUDGET_SECONDS=90
MAX_FILE_SIZE_MB=10
UPLOAD_DIR=uploads
CORS_ORIGINS=http://localhost:3000
